{
  "name": "calculator-pg",
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ./styles.css --minify",
    "dev": "tailwindcss -i ./input.css -o ./styles.css --watch"
  }
}
